{% extends "/app/Resources/views/base.html.twig" %}

{% block title %} {{ title }} {% endblock %}

{% block stylesheets %}
	
	{{ parent() }}

	<!-- WEBPACK BUNDLE -->
	<link rel="stylesheet" href="{{CDN('stylesheet')}}/demoBundle/assets/css/demo.css" />
	
{% endblock %}

{% block javascripts %}
	
	{{ parent() }}

	<!-- WEBPACK BUNDLE -->
	<script src="{{CDN('javascript')}}/demoBundle/assets/js/demo.js"></script>

	
	<script>
		/* ---------------------------------------------- /*
		 * LANG IN URL CHANGE
		/* ---------------------------------------------- */
		$("#langs").bind("change",function(){
			window.location.href = "?lang="+$(this).val();
		})
	</script>
{% endblock %}

{% block body %}
	<header class="header">
		{{ render( controller("demoBundle:demo:nav", login )) }}
	</header>
{% endblock %}

{% block footer %}
	<footer  >
		{{ render( controller("demoBundle:demo:footer" )) }}
	</footer>
{% endblock %}

